客户端：
1. 【已修】【提示】：注册新账号登录后没有组织进入app后需要在首页显示创建组织或加入组织入口，当前需要去设置中找
   - 修改进度：已完成。
   - 修改方案：首页无组织状态增加“创建或加入组织”入口，引导用户进入组织创建/邀请码加入流程。
2. 【已修】【一般】：创建公寓是选择日期面板被其他输入框遮挡
   - 修改进度：已完成。
   - 修改方案：移动端 Web 日期选择器改为独立 Modal 弹层，避免被表单输入框遮挡。
3. 【已修】【一般】：签约入住页面选择日期面板被其他输入框遮挡
   - 修改进度：已完成。
   - 修改方案：签约表单复用新的独立日期弹层。
4. 【已修】【严重】：有组织编码就能随意加入组织，不安全
   - 修改进度：已完成。
   - 修改方案：组织编码仅用于识别组织；加入组织改为管理员生成的一次性邀请码，默认 24 小时有效且只能使用一次。
5. 【已修】【严重】：新加入的成员只有只读权限，后端有控制但前端没做限制
   - 修改进度：已完成。
   - 修改方案：前端按角色权限隐藏或拦截公寓、房间、租约等管理操作，并在无权限时提示。
6. 【已修】【一般】：操作无权限时系统没有任何提示；系统需要对操作有反馈
   - 修改进度：已完成。
   - 修改方案：移动端关键操作补充 try/catch 和 notice 提示，后端拒绝时前端展示错误原因。
7. 【已修】【提示】：登录后header中的下拉菜单会自动打开；预期：不要打开
   - 修改进度：已完成。
   - 修改方案：header 用户菜单默认关闭，并在登录用户变化或切换 tab 时强制关闭，避免残留展开状态。
8. 【已修】【一般】：没有删除组织功能
   - 修改进度：已完成。
   - 修改方案：组织设置中增加所有者删除组织入口，按组织名二次确认，沿用后端有效订阅不可删除规则。
9. 【已修】【一般】：添加费用项按钮超出卡片，需要修改样式
   - 修改进度：已完成。
   - 修改方案：调整 section header、按钮和操作区换行/收缩样式，按钮不再挤出卡片。
10. 【已修】【提示】：创建重复房间系统没有任何提示
   - 修改进度：已完成。
   - 修改方案：单个新增房间根据后端 `createMany.count` 判断是否实际创建，重复时提示“房间号已存在，未重复创建”；批量创建继续提示重复会自动跳过。
11. 【已修】【严重】：退租流程有问题，退租时只对房间做了处理，没有对已有账单和退租时可能发生的费用进行处理，比如退押金、退房租、进行水电费结算等
   - 修改进度：已完成。
   - 修改方案：新增退租结算单，支持押金退还/扣款、手动房租退补、退租水电读数、其他费用、应收/应退净额，以及结算收/退款记录。
12. 【已修】【提示】：到期解约退租日期填写早于原租约结束日期时系统没有提示；
   - 修改进度：已完成。
   - 修改方案：后端保留日期校验，前端退租结算提交时捕获错误并展示提示。
13. 【已修】【一般】：账单中看不了账单明细
   - 修改进度：已完成。
   - 修改方案：月度账单卡片展示子账单、账单项明细和金额。
14. 【已修】【一般】：水电录入操作太复杂了，这是最常用的操作应该操作简单一些；而且账单中不能直接填写水电记录，使用很不方便；
   - 修改进度：已完成。
   - 修改方案：账单卡片和出账处理卡片增加“录入本期水电”入口，直接按账单填写上期/本期水电读数并更新水电账单。
15. 【已修】【严重】：对一个房间收款后再次点击登记收款，不选择账单可直接填写收款，账单依然会累积显示已收款；
   - 修改进度：已完成。
   - 修改方案：收款必须绑定未结清月度账单，默认选中第一张待收账单，后端限制不能对已结清账单继续收款且不能超过剩余应收。
16. 【已修】【一般】：缺少收款记录功能
   - 修改进度：已完成。
   - 修改方案：月度账单展示收款记录；退租结算新增收款/退款记录模型和接口。
17. 【已修】【一般】：缺少水电导入导出功能
   - 修改进度：已完成。
   - 修改方案：账单抄表页增加待录入水电 CSV 导出和 CSV 粘贴导入；后端导入统一按 `billId` 更新后付费水电账单。

管理端
1. 【已修】【提示】：系统缺少提示，登录输入错的密码接口响应了手机号或密码不正确，但前端没有反应
   - 修改进度：已完成。
   - 修改方案：运营端登录、注册、验证码操作补充错误捕获和 message 提示。
2. 【已修】【一般】使用业主账号登录后，修改成员账号为运营人员后报错，提示无运营权限；但退出后再次登录已无法登录；此时使用成员账号可以登录运营平台、可修改用户权限；超级管理员权限不应该能转让；
   - 修改进度：已完成。
   - 修改方案：初始化管理员首次修改运营权限时先固化为 `SUPER_ADMIN`；仅超级管理员可修改运营权限，防止权限被隐式转让。
3. 【已修】【严重】：用户没购买套餐依然能创建公寓，购买公寓数量为1的套餐可创建两个公寓
   - 修改进度：已完成。
   - 修改方案：后端增加套餐额度校验，未购套餐不能创建公寓，公寓/房间/成员按套餐和额度包限制。
