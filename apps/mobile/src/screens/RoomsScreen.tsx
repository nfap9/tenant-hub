import { Text, View } from "react-native";
import { styles } from "../styles";

export default function RoomsScreen() {
  return (
    <View style={styles.panel}>
      <Text style={styles.muted}>房间列表功能开发中</Text>
    </View>
  );
}
